#include "holberton.h"
/**
 * mul - Funcion that multiples 2 digits. Start point.
 * 
 *@a: digit to be  multipled. 
 *@b: second digit tobe multipled.
 *
 *Return: result.
 */
int mul(int a, int b)
{
	int result = 0;

	result = (a * b);

	return (result);
}
